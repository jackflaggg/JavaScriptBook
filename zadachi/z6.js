/** ЗАДАЧА 6 - Типы данных
 *
 * 1. Объявите несколько переменных и присвойте им значения:
 *  - строка
 *  - число
 *  - логическое
 *  - null
 *  - undefined
 *  - объект
 *  - массив
 *
 * 2. Выведите в консоль тип каждого из значений
 * используя оператор typeof
 */

const onePerem = 'строка'
const twoPerem = 12
const threePerem = false
const fourPerem = null
let fivePerem
const sixPerem = {}
const sevenPerem = []

console.log(typeof(onePerem))
console.log(typeof(twoPerem))
console.log(typeof(threePerem))
console.log(typeof(fourPerem))
console.log(typeof(fivePerem))
console.log(typeof(sixPerem))
console.log(typeof(sevenPerem))
