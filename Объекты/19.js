function sayHi(bj) {   // (1) создаём
    console.log( bj );
}

let func = sayHi;    // (2) копируем

func('Пока'); // Привет     // (3) вызываем копию (работает)!
sayHi('Ghbdtn'); // Привет    //     эта тоже все ещё работает (почему бы и нет)